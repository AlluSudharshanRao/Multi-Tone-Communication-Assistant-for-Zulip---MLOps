# Kustomize bundle: MLflow Deployment + PVC + Service + Ingress (namespace ml-platform).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ml-platform
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
