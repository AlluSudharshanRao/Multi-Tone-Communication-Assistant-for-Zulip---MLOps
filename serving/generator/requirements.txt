# Core serving (always required)
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
numpy==1.26.4
prometheus-client==0.20.0
httpx==0.27.0

# Required only when DUMMY_MODE=false (real LLM inference)
# Uncomment if you set DUMMY_MODE=false:
# transformers==4.40.0
# torch==2.2.2
# accelerate==0.29.3
# better-profanity==0.7.0
